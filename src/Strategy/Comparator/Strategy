### 概述：
做一件事情可以有很多种策略。

比如，“将很多人排序”，可以基于不同的策略
> 1.按照财产排序（财产越高，排行越高）

> 2.按照身高排序（身高越矮，排行越高）

这时候sort这个事情在不同的场合下可能要按照不同的策略来。

因此我们可以定制不同的策略，并将选择的策略传入sort方法，sort方法按照策略进行排序。

对于所有的策略，它们在一定程度上必须相同（标准）:

> 返回值-1，则必然...

> 返回值0，则必然...

> 返回值1，则必然...
